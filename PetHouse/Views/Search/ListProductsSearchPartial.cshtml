@model List<Product>
@if (Model != null)
{
    foreach (var item in Model)
    {
        <tr>          
            <td>
                @item.Name
            </td>

            <td>@item.OrderPrice.ToString("#,##0") VNĐ</td>
        </tr>
    }
}